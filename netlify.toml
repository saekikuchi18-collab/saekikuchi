[build]
  command = "pnpm build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# Redirect for Netlify Identity login callback
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200
